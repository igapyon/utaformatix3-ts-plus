# mikuscore 改善候補バックログ（下書き）

この文書は、`utaformatix3-ts-plus` 実装中に見つかった `mikuscore` 側改善候補を蓄積するための下書き。

## 候補一覧

## 1. MusicXML import正規化APIの公開分離

- 背景:
  - `plus` 側では `normalizeImportedMusicXmlText` フック経由で再利用したい。
- 提案:
  - `mikuscore` 側で正規化処理を独立した公開関数として提供する。
- 期待効果:
  - 連携先で同一正規化を再利用しやすくなる。

## 2. diagnostics payload共通ヘルパー化

- 背景:
  - `diag:*` 記録のフォーマットを複数I/O間で揃えたい。
- 提案:
  - キー順・payload整形を共通ユーティリティ化する。
- 期待効果:
  - 解析ツール側での取り扱いが容易になる。

## 3. Node/Browser境界の明示

- 背景:
  - DOM依存境界が暗黙だと連携時に差し替え判断が難しい。
- 提案:
  - DOM必須モジュールと純ロジックモジュールを明示的に分離する。
- 期待効果:
  - ランタイム差し替えとテスト設計が安定する。

## 4. 変換report APIの標準化

- 背景:
  - `plus` では `WithReport` APIを導入し、warning/error蓄積で運用している。
- 提案:
  - `mikuscore` 側I/Oでも同様の戻り値規約を定義する。
- 期待効果:
  - UI/CLI双方でエラー制御を一貫化できる。

## 運用ルール

- 新候補を追加する際は「背景 / 提案 / 期待効果」の3点を必ず記載する。
- issue化時は、この文書の項目IDを参照してトレース可能にする。

